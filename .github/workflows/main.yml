name: Test

on:
  push:
    branches: [ master ]

jobs:
  build:
    runs-on: windows-latest

    steps:
    - uses: actions/checkout@v2
    
    - name: clang cl
      run: |
        cmake --help
        
    - name: check
      run: |
        Get-ChildItem -Path "C:\msys64\mingw64\bin"
        
    - name: env
      run: env
        
