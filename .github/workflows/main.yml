name: Test

on:
  push:
    branches: [ master ]

jobs:
  build:
    runs-on: windows-latest

    steps:
    - uses: actions/checkout@v2
        
    - name: Validate Newman
      run: |
        Write-Host "Newman $(newman --version)"
        
