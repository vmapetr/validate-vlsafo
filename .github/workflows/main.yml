name: Test

on:
  push:
    branches: [ master ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    
    - name: Show dotnet sdks and runtimes
      run: |
        dotnet --version
        dotnet --list-sdks
        dotnet --list-runtimes
    
    - name: Setup dotnet
      run: |
        source ./dotnet-install.sh --version 2.1.202
        echo $PATH
        dotnet --version
    
    - name: Show dotnet sdks and runtimes
      run: |
        dotnet --version
        
    - name: ls
      run: ls /usr/share/dotnet/sdk
    
    - name: env
      run: env
