name: Test

on:
  push:
    branches: [ master ]

jobs:
  build:
    runs-on: windows-latest

    steps:
    - uses: actions/checkout@v2
    
    - name: Test
      run: |
        & "C:\Program Files (x86)\Microsoft Visual Studio\2019\Enterprise\VC\Tools\Llvm\bin\lld-link.exe" -help
        
    - name: check
      run: |
        Write-Host "-----------"
        Get-ChildItem -Path "C:\msys64\mingw64\bin"
        Write-Host "-----------"
        Get-ChildItem -Path "C:\Program Files\Git\mingw64\bin"
        
