name: Test

on:
  push:
    branches: [ master ]

jobs:
  build:
    runs-on: macos-latest

    steps:
    - uses: actions/checkout@v2
    
    - name: Install Newman
      run: |
       brew install newman
        
    - name: Validate Newman
      run: |
        Write-Host "Newman $(newman --version)"
        
